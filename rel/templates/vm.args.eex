## Name of the node
-name <%= release_name %>@<%= "${POD_A_RECORD}.${NAMESPACE}.pod.cluster.local" %>

## Cookie for distributed erlang
-setcookie ${ERLANG_COOKIE}

# Enable SMP automatically based on availability
-smp auto
